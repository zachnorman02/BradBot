version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: bradbot
      POSTGRES_USER: bradbot
      POSTGRES_PASSWORD: bradbot_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
